{
  "only": ["ts", "json", "md", "yml"],
  "exclude": ["dist", "coverage", "node_modules", ".release"],
  "depth": 4,
  "routing": {
    "subsystems": [
      {
        "name": "context-scope-contract",
        "paths": ["src/index.ts", "src/ssr.ts", "tests/"],
        "keywords": ["ContextToken", "runWithContext", "setSsrContextMap", "_enterContext"],
        "docs": ["README.md"],
        "note": "The client prototype-chain scope and flat SSR map are two implementations of the same public provide/inject contract; the SSR subpath is a re-export seam that must remain behaviorally live."
      }
    ]
  }
}
